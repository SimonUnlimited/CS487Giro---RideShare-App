import 'package:flutter/material.dart';

class MyButton extends StatelessWidget {
  int height;
  int width;
  String content;

  MyButton(int height, int width, String content) {
    this.height = height;
    this.width = width;
    this.content = content;
  }
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}